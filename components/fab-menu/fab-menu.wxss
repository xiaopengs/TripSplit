/* === FAB 悬浮按钮 — 圆形 + 号 === */

.fab-btn {
  position: fixed;
  right: 36rpx;
  bottom: calc(48rpx + env(safe-area-inset-bottom));
  z-index: 999;
  width: 104rpx;
  height: 104rpx;
  border-radius: 50%;
  background: #34C759;
  display: flex;
  align-items: center;
  justify-content: center;
  box-shadow: 0 8rpx 24rpx rgba(52, 199, 89, 0.4);
}

.fab-btn-hover {
  opacity: 0.85;
  transform: scale(0.94);
}

.fab-plus {
  color: #FFFFFF;
  font-size: 52rpx;
  font-weight: 300;
  line-height: 1;
}

/* === 以下为展开面板样式（MVP 暂不使用，后续恢复用） ===
.fab-overlay {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.45);
  z-index: 998;
  opacity: 0;
  pointer-events: none;
  transition: opacity 200ms ease;
}

.fab-overlay.show {
  opacity: 1;
  pointer-events: auto;
}

.fab-pill {
  position: fixed;
  right: 32rpx;
  bottom: calc(40rpx + env(safe-area-inset-bottom));
  z-index: 999;
  display: flex;
  align-items: center;
  background: #FFFFFF;
  border-radius: 60rpx;
  padding: 16rpx 28rpx 16rpx 16rpx;
  box-shadow: 0 8rpx 32rpx rgba(0, 0, 0, 0.15), 0 2rpx 8rpx rgba(0, 0, 0, 0.08);
  transition: all 250ms var(--spring-curve);
}

.fab-pill.expanded {
  background: var(--color-danger);
  box-shadow: 0 8rpx 32rpx rgba(255, 59, 48, 0.35);
  padding-right: 20rpx;
}

.fab-options {
  position: fixed;
  right: 32rpx;
  bottom: calc(140rpx + env(safe-area-inset-bottom));
  z-index: 999;
  width: 480rpx;
  background: #FFFFFF;
  border-radius: var(--border-radius-lg);
  box-shadow: 0 16rpx 48rpx rgba(0, 0, 0, 0.15);
  opacity: 0;
  transform: translateY(20rpx) scale(0.95);
  pointer-events: none;
  transition: all 250ms var(--spring-curve);
  overflow: hidden;
}

.fab-options.show {
  opacity: 1;
  transform: translateY(0) scale(1);
  pointer-events: auto;
}

.fab-option-item {
  display: flex;
  align-items: center;
  padding: 28rpx 28rpx;
  border-bottom: 1rpx solid var(--border-color);
}

.fab-option-item:last-child {
  border-bottom: none;
}
*/
